[package]
name = "zaydo/fishing-game-demo"
version = "0.1.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

[dependencies]

[server-dependencies]
# Server-only data packages belong here so clients never receive persistence modules.
ProfileStore = "2jammers/profilestore@0.1.0"
